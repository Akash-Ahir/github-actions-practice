name: Smart Triggers

on:
  push:
    branches:
      - main
      - 'release/*'
    paths:
      - 'src/**'
      - 'app/**'

jobs:
  run-on-code-changes:
    runs-on: ubuntu-latest
    steps:
      - name: Print changed files
        run: git diff --name-only HEAD~1 HEAD


name: Daily Schedule

on: 
  schedual:
    - cron: '0 0 * * *'
  workflow_dispatch:
jobs:
  schedual-job:
    runs-on: ubuntu-latest
    steps:
      - print schedual message
        run: echo "workflow run everyday at mid night"

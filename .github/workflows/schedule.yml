name: Daily Schedule

on: 
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  schedule-job:
    runs-on: ubuntu-latest
    steps:
      - name: print schedule message
        run: echo "workflow run everyday at mid night"
